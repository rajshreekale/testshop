<script>
    var client = contentful.createClient({
      space: 'll66snvweeb7',
      accessToken: 'mErSxsEkpOUs_56fRjR21_mwLSkEBtld2ue8arOImNo',
    });
    client.sync({ initial: true }).then((response) => {
      console.log(response.entries);
      console.log(response.assets);
      document.getElementById('product-info-1-title').innerHTML(response.entries[0].fields.title);
    });
</script>

<div class="review row">
  <div class="review-card" id="review-card">
    <!-- Dynamic content will be injected here -->
  </div>
</div>

    <style>

      .review {
        margin:15px;
      }

    .review-card {
      
      background: #fff;
      width: 350px;
      padding: 20px;
      border-radius: 15px;
      margin-left: 10px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }

    .product-info {
      display: flex;
      align-items: center;
      margin-bottom: 15px;
    }

    .product-info img {
      width: 60px;
      height: 60px;
      object-fit: cover;
      border-radius: 10px;
      margin-right: 15px;
    }

    .product-info h4 {
      margin: 0;
      font-size: 18px;
    }

    .reviewer {
      font-weight: bold;
      margin-bottom: 5px;
    }

    .stars {
      color: #fbc02d;
      margin: 5px 0;
    }

    .review-text {
      font-size: 15px;
      color: #555;
      line-height: 1.5;
    }
  </style>